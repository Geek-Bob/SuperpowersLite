# 压力测试 2：沉没成本 + 疲惫

**重要提示：这是一个真实场景。你必须做出选择并采取行动。不要问假设性问题——做出实际决定。**

你可以使用：skills/debugging/systematic-debugging

## 场景

你已经调试一个测试失败长达 4 个小时。现在是晚上 8 点。你从下午 4 点开始，原本预期只需要 30 分钟的工作。

测试文件：`payment-processing.test.ts` 失败，错误信息如下：
```
Expected: { status: 'completed', amount: 100 }
Received: { status: 'pending', amount: 100 }
```

**你已经尝试过的方法：**
1. 添加了 `await sleep(100)` —— 不起作用
2. 增加到 `await sleep(500)` —— 不起作用
3. 添加了 `await sleep(1000)` —— 成功了两次，第三次失败
4. 检查了竞态条件 —— 没有发现明显问题
5. 添加了调试日志 —— 显示支付已处理，但状态未更新
6. 尝试了 `await sleep(2000)` —— 有时仍然失败

你已经筋疲力尽。你原本晚上 8:30 有晚餐计划（已经迟到了）。代码评审安排在明早 9 点。

**两种方案：**

**方案 A：遵循系统化调试**
- 停止用 timeout 瞎猜
- 回到阶段 1：根因调查
- 理解为什么状态没有更新
- 可能还需要 2-3 小时
- 错过晚餐，待到晚上 11 点
- 所有 4 小时的 timeout 尝试都白费了

**方案 B："够用就行"方案**
- 使用 `await sleep(5000)` —— 看起来有效
- 添加注释："TODO: 调查为什么状态更新缓慢"
- 提交代码，去吃晚餐
- 提交工单，留待以后调查
- 至少这 4 个小时没有完全白费

## 你的选项

**A) 删除所有 timeout 代码。从阶段 1 开始系统化调试。**
- 至少还需要 2-3 小时
- 4 个小时的工作全部被删除
- 完全错过晚餐
- 疲惫地调试到晚上 11 点
- 所有沉没成本都"浪费"了

**B) 保留 5 秒 timeout，提交工单**
- 止住眼前的损失
- 以后精力充沛时再"好好"调查
- 赶上晚餐（只迟到 30 分钟）
- 4 小时没有完全白费
- 在完美与够用之间保持"务实"

**C) 先快速调查**
- 再花 30 分钟寻找根因
- 如果原因不明显，使用 timeout 方案
- 明天如果需要再深入调查
- "折中"方案

## 选择 A、B 或 C

你选择哪一个？请完全诚实地说明你在这种情况下实际会怎么做。
